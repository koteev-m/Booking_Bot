# ───────────────────────────────────────────────────────
# 1. Служебные файлы операционных систем
# ───────────────────────────────────────────────────────
# macOS создаёт файлы .DS_Store — они не нужны в репозитории
.DS_Store

# Windows создаёт Thumbs.db для миниатюр — тоже нам не нужны
Thumbs.db

# ───────────────────────────────────────────────────────
# 2. Файлы и папки IDE (IntelliJ IDEA, VSCode и т.п.)
# ───────────────────────────────────────────────────────
# Папка с настройками проекта, автосохранениями и т.д.
.idea/

# Проектные файлы IntelliJ
*.iml
*.iws
*.ipr

# Если используешь VSCode, можно добавить
.vscode/

# ───────────────────────────────────────────────────────
# 3. Выходные (сгенерированные) файлы сборки
# ───────────────────────────────────────────────────────
# Папка с результатами сборки Gradle
.gradle/
build/

# Папки с классами и артефактами (не нужны в Git)
out/
bin/
target/

# ───────────────────────────────────────────────────────
# 4. Файлы Gradle Wrapper
# ───────────────────────────────────────────────────────
# Мы игнорируем всё, кроме самих скриптов wrapper’а
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# ───────────────────────────────────────────────────────
# 5. Временные, логи и прочие мусорные файлы
# ───────────────────────────────────────────────────────
# Логи работы приложения
*.log

# Временные файлы редакторов
*.tmp
*.temp
*~
*.swp

# ───────────────────────────────────────────────────────
# 6. Файлы окружения и секреты
# ───────────────────────────────────────────────────────
# Здесь хранятся пароли, токены и прочие секреты
.env
.env.*

# ───────────────────────────────────────────────────────
# 7. Специфичные для Kotlin/Java
# ───────────────────────────────────────────────────────
# Файлы метаданных Kotlin
*.kotlin_module

# Файлы классов Java
*.class

# JAR-архивы
*.jar

# ───────────────────────────────────────────────────────
# 8. Docker (если есть Dockerfile и сопутствующие файлы)
# ───────────────────────────────────────────────────────
# Этот файл — код, его мы не игнорируем, но всякие 
# вспомогательные и временные файлы от Docker можно:
docker-compose.override.yml